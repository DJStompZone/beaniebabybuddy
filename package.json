{
	"name": "beaniebabybuddy",
	"version": "1.0.6",
	"private": false,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"build": "wrangler build",
		"push": "git add . ; git commit && npm version patch && git push"
	},
	"devDependencies": {
		"wrangler": "^3.101.0"
	}
}
